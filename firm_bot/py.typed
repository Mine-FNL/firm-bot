# Marker file for PEP 561 — see https://peps.python.org/pep-0561/
# Tells type checkers (mypy, pyright) that firm_bot ships inline type
# hints. Pair this with `package-data = {firm_bot = ["py.typed"]}` in
# pyproject.